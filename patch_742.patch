--- a/S.P.L.U.R.T-tg/src/main/java/com/splurt/station/bugfix/Task742.java
+++ b/S.P.L.U.R.T-tg/src/main/java/com/splurt/station/bugfix/Task742.java
@@ -10,23 +10,25 @@
 import java.util.logging.Logger;
 
 public class Task742 {
-    private static final Logger logger = Logger.getLogger(Task742.class.getName());
+    private static final Logger LOGGER = Logger.getLogger(Task742.class.getName());
     private final String input;
-
+    
     public Task742(String input) {
         this.input = input;
     }
 
     public void processInput() {
-        logger.info("Processing input: " + input);
+        if (input == null || input.isEmpty()) {
+            LOGGER.warning("Input is empty or null. Exiting.");
+            return;
+        }
+
+        LOGGER.info("Processing input: " + input);
         int counter = 0;
-        while (counter < input.length()) {
-            char c = input.charAt(counter);
-            if (c == ' ') {
-                // Do something
-            }
-            counter++;
+        
+        for (int i = 0; i < input.length(); i++) {
+            char currentChar = input.charAt(i);
+            processCharacter(currentChar, i);
         }
     }
 
@@ -34,6 +36,18 @@
             System.out.println("Character is a space: " + c);
         } else if (Character.isLetter(c)) {
             // Handle letters
-            // More processing needed here
+        }
+
+    private void processCharacter(char c, int position) {
+        try {
+            if (c == ' ') {
+                handleSpace(position);
+            } else if (Character.isLetter(c)) {
+                handleLetter(c, position);
+            } else {
+                // Handle other characters or ignore
+            }
+        } catch (Exception e) {
+            LOGGER.severe("Error processing character at position " + position + ": " + e.getMessage());
         }
     }